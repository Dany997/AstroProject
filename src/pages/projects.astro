---
const response = await fetch('https://astroproject-production.up.railway.app/api/projects')
const projects: { id: string; title: string; description: string }[] = await response.json();

import NewLayout from "../layouts/newLayout.astro";
---



<NewLayout>
  <section class="projects">
    <h1>Moje projekty</h1>
    <ul>
      {projects.map((project: { id: string; title: string; description: string }) => (
        <li>
          <a href={`/projects/${project.id}`}>
            <h2>{project.title}</h2>
            <p>{project.description}</p>
          </a>
        </li>
      ))}
    </ul>
  </section>
</NewLayout>